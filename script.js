for (let i = 0; i < 1500; i++) {
    const span = document.createElement('span')
    console.log('con is ',document.querySelector('.container'))
    document.querySelector('.container').append(span)
    
}